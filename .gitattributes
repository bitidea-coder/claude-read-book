# Force LF for shell scripts and python so the plugin works on macOS/Linux
# regardless of the contributor's platform (CRLF breaks bash shebang parsing).
*.sh text eol=lf
*.py text eol=lf
*.json text eol=lf
*.md text eol=lf
